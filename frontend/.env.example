# Frontend environment template — only VITE_* keys are read by the app
# (see src/config/env.ts). Copy to frontend/.env.
# See ../.env.example for the shared, full template and ../README.md for setup steps.

VITE_API_URL=http://localhost:8000
VITE_DEMO_MODE=true
